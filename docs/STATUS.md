# NOVA — состояние проекта и план вперёд

Обновлено: 2026-07-03 (конец первой большой сессии разработки).

## Что такое NOVA

Персональная голосовая ИИ-компаньонка: видит экран, комментирует сама, ведёт
голосовой диалог, без цензуры, self-hosted. Голос — Мита из MiSide.
Спека: `docs/specs/2026-07-03-nova-companion-design.md`.
Планы: `docs/plans/` (этап 1 — скелет, этап 2 — первый голос; оба ВЫПОЛНЕНЫ).

## Текущее состояние: РАБОТАЕТ

Этапы 1 и 2 завершены. Живой прогон прошёл: NOVA говорит клонированным
голосом, видит экран (описывает рабочий стол, YouTube, обои), помнит диалог
в рамках сессии, комментирует сама. Все ~70 тестов зелёные.

### Архитектура (кратко)

- **Клиент (ноут, Windows)**: `nova/client/` — захват экрана и микрофон в
  ОТДЕЛЬНЫХ процессах (нативные DLL конфликтуют в одном), детектор смены
  сцены, стриминговое воспроизведение, хоткеи (pynput), реконнект.
- **Сервер (аренда GPU на Vast.ai)**: `nova/server/` — FastAPI-оркестратор,
  vLLM с Qwen3-VL-30B-A3B-FP8 (мозг+глаза, порт 5000), faster-whisper
  large-v3-turbo (уши), XTTS-v2 (голос, клон из voice_sample.wav).
- Связь: WebSocket, JSON + base64, токен-аутентификация (NOVA_TOKEN).

### Инфраструктура и операции

- Секреты в `.env` (не в git): VAST_API_KEY, NOVA_TOKEN.
- Управление сервером: `uv run python scripts/vast.py up|status|down [--destroy] [--write-config]`
- Сейчас есть ОСТАНОВЛЕННЫЙ инстанс id=43664615 (RTX 6000 Ada, Австралия,
  $0.516/ч, ssh2.vast.ai:24614) с прогретым кэшем моделей — `up` поднимет
  его за 2–3 минуты. Хранение диска ~$0.15/день. Баланс Vast: ~$13.85.
- SSH-доступ к инстансу: ключ `~/.ssh/nova_vast` (уже прикреплён).
- Деплой обновлений на живой инстанс:
  `ssh -i ~/.ssh/nova_vast -p <port> root@<host> "cd /workspace/NOVA && git pull -q; pkill -f '[n]ova.server.main'; pkill -f '[i]dle_watchdog'; sleep 1; (setsid nohup bash /root/runner.sh > /var/log/runnerN.log 2>&1 < /dev/null &)"`
  (`/root/runner.sh` лежит на инстансе: подхватывает env из /proc/1/environ,
  ждёт vLLM, стартует оркестратор + автостоп-вотчдог).
- Логи на инстансе: /workspace/vllm.log, /workspace/nova.log,
  /var/log/onstart.log, /var/log/runnerN.log.
- Автостоп: 15 минут без клиентов → инстанс останавливает себя сам.

### Грабли, которые уже пойманы (НЕ наступать заново)

1. Vast API — солянка версий: список инстансов GET /api/v1/instances/,
   а состояние/удаление/ssh — на /api/v0/. Уже учтено в scripts/vast.py.
2. Фильтры выбора карты (все в pick_offer): не Китай (нет доступа к
   HF/GitHub), compute_cap >= 800 (FP8 требует Ampere+), cpu_arch amd64
   (наш образ x86), надёжность >= 0.98, канал >= 200 Мбит, диск >= 80 ГБ.
3. На инстансах НЕТ /workspace по умолчанию — onstart.sh делает mkdir.
4. XTTS ломается на русских фразах длиннее ~182 символов — сервер режет
   текст по предложениям (split_for_tts) и синтезирует максимум 5.
5. Повторы в диалоге («целая палитра» N раз) — лечатся presence_penalty=0.8
   + frequency_penalty=0.6 в qwen_llm + правила в системном промпте.
6. Обрезка ответа по токенам рвала фразы — trim_to_sentence отсекает хвост.
7. Звуковая петля (слышит себя из колонок) — клиент глушит микрофон, пока
   NOVA говорит (+1 сек после). В наушниках проблемы нет вовсе.
8. В голосовые ответы НАДО прикладывать свежие кадры (2 шт) — иначе она
   сочиняет про экран. Уже сделано (build_reply_messages).
9. На ноуте: dxcam/PortAudio/onnxruntime в одном процессе = access
   violation → захват и микрофон живут в дочерних процессах. Библиотека
   keyboard битая — заменена на pynput.
10. Windows CRLF ломает bash-скрипты — .gitattributes: *.sh eol=lf, плюс
    vast.py нормализует onstart при отправке.
11. `pkill -f '[v]llm serve'` убивает только API-сервер vLLM; дочерний
    движок переименовывает себя в `VLLM::EngineCore` (ВЕРХНИЙ регистр!)
    и сиротой держит ~31 ГБ VRAM. Добивать: `pkill -9 -f '[V]LLM'`.
12. Для дообучения GPU нужен целиком: глушить vllm+fish+оркестратор
    (и помнить про п.11). Вачдог тренировку не убьёт (проверяет
    gpu_work_running), но onstart при пробуждении инстанса поднимает
    весь стек заново — глушить ПОСЛЕ пробуждения, не до.
13. Переобучение TTS-файнтюна: 1500 шагов на 19 мин датасета (~11 эпох) —
    модель зазубрила клипы и потеряла стоп-токен (фраза 6 с → 142 с
    бормотания). Норма: 300–500 шагов (2–4 эпохи), сохранять все чекпоинты
    (callbacks.model_checkpoint.save_top_k=-1 — иначе ротация оставит
    только последние 5) и выбирать на слух/по длине аудио.

### Известные проблемы (не критичные)

- Голос XTTS: роботность и лёгкие искажения — главный кандидат на замену
  (см. план «Голос 2.0» ниже).
- Между предложениями длинного ответа бывают паузы синтеза.
- Проактивные комментарии пока редкие/простые — тюнить болтливость в
  profiles/*.yaml по вкусу.

## ПЛАН ВПЕРЁД (по приоритету)

### «Голос 2.0» — ВЕРДИКТ (2026-07-03, вечер)

- **Прод: fish S1-mini + 28с нормализованный референс** (6 клипов, loudnorm,
  44.1кГц) — лучший результат по ушам пользователя. XTTS побеждён.
- Претензии к проду: ударения, местами интонация, лёгкая «грязь»,
  цифры/латиница (цифры/латиница решены — tts_text.py нормализует).
- **Файнтюн S1-mini — ЗАКРЫТ**: LoRA сломана у апстрима (issues #1136,
  #1120, loss≈19 и не падает); полный файнтюн учится (loss 7→4.5), стоп
  работает, но звук мусорный и на тихом, и на нормализованном датасете —
  потолок сырья (пережатые короткие ogg). Не тратить GPU без новостей
  апстрима. Датасет и конвейер сохранены: /workspace/mita_data (лапки+wav
  нормализованные), deploy/finetune_prep.py, scripts/sort_voices.py.
- **Следующий шаг (решение пользователя)**: облачный fish.audio,
  СНАЧАЛА бесплатная s2.1-pro-free (то же качество, что платная; окно
  до конца июля 2026!), готовая модель Миты reference_id
  6dc11f3f67a543f6ad4537a4a347e224. Режим NOVA_TTS=fishcloud ГОТОВ в коде:
  ключ пользователя положить в /workspace/fish_key, перезапустить runner.
  Если понравится звук, но не скорость/приватность — платный PAYG
  ($15/М байт ≈ $5–27/мес при его стиле).
- Запас self-hosted: F5-TTS с русскими весами (план Б-2 ниже).

### ТЕКУЩИЙ ИНСТАНС (5-й, день 04.07): id=43793150, RTX A6000 48ГБ,
### $0.436/ч, ssh6.vast.ai:33150, ws://72.83.150.152:52314/ws
(4-й погиб: сосед-арендатор занял GPU, очередь не двигалась 30+ мин,
диск был забит 80/80 ночными чекпоинтами → destroy+up на чистом.
Кнопки пользователя: NOVA_START.bat / NOVA_STOP.bat в корне проекта.
⚠ У хоста 5-го инстанса техработы 4–6 августа 2026 — если к тому времени
ещё на нём, пересоздаться заранее.)

### МОЗГ 2.0 — ПЕРЕСАДКА УДАЛАСЬ (ночь 04.07, инстанс 43751526)

- **Qwen3.6-27B-FP8 (плотная, мультимодальная) вместо 30B-A3B MoE** —
  диагноз «тонкая MoE не обобщает правила, конфабулирует, ломает
  грамматику вне шаблонов» подтверждён и вылечен. Чек-лист внешаблонных
  проверок пройден НА ОТЛИЧНО: ложная память отбита под дожимом («не
  вешай на меня чужие слова»), нить/авторство держит, телепатию честно
  отклоняет, провокации гасит теплом, счёт словами, шёпот/злость
  исполняет ремарками, «одним словом» умеет. Пользователь счастлив.
- Образ vllm/vllm-openai:v0.19.0 (3.6 требует >=0.19), RTX PRO 5000
  Blackwell 48ГБ $0.578/ч. Настройки vLLM: max-len 24576, util 0.85,
  --enforce-eager (профиль cuda-графов OOM'ится), картинок 8
  (NOVA_IMG_LIMIT, orchestrator режет кадры под лимит). Ответ ~0.5с.
- enable_thinking=False в chat-запросах (qwen_llm) — иначе думает вслух.
- Грабли новые: ssh-ключ цепляется К КАЖДОМУ инстансу заново (POST
  /api/v0/instances/{id}/ssh/); хост подсовывает HF_ENDPOINT-зеркало
  (Китай) в ssh-сессии — лечится HF_HUB_OFFLINE=1 при кэше/unset.

### НОЧЬ 04.07 — автономные операции (пользователь спит)

- **СТТ-страж работает в проде**: каждое предложение синтеза сверяется
  виспером с текстом, заскок → тихий пересинтез temp+0.15. Открытие
  пользователя: заскок детерминирован по (тексту × движку) — у двух разных
  моделей голоса в одном месте. tts_text.speech_matches, порог 0.6.
- Qwen3-TTS: лучшая связка r28+seed123+temp0.6; вердикт «гарнитура против
  студии облака» → ЧЕРНОВИК-запасной. AP-BWE 24→48к работает (~0.5с/файл,
  /workspace/apbwe_*), «студийность» апсемпла пользователь оценить не успел.
- **F5-файнтюн УШЁЛ В НОЧЬ НА ПРОДЕ**: NOVA-сервисы остановлены по просьбе
  пользователя, GPU целиком под тренировку (batch 3200, 120 эпох, от
  русского чекпоинта, метадата со 100% ударений — RUAccent на проде с
  monkey-патчем token_type_ids). По завершении инстанс ГАСИТ СЕБЯ САМ
  (f5_train_prod.sh). Чекпоинты: /workspace/ckpts/.
- Трен-коробка 3090 УНИЧТОЖЕНА (гнилая сеть: HF капал, прокси рвал).
  Схема «подготовка там, тренировка тут» доехала через ноут-мост.
- **Датасет теперь полностью на ноуте**: voices/f5data_full.tgz (557 wav
  24кГц нормализованные + .lab + metadata с ударениями) — невосполнимого
  на инстансах НЕТ.
- **ТРЕНИРОВКА F5 ЗАВЕРШЕНА УСПЕШНО** (вся заняла ~10 мин на PRO 5000!).
  Грабли ночи: (а) namespace-пакет f5_tts → __file__ None → пути искать
  find'ом; (б) конвенция путей: data/ckpts на уровне lib/python3.12/,
  имя каталога = {dataset}_{tokenizer}; (в) претрейн-чекпоинт несёт
  счётчик update=414к → тренер мгновенно «завершается» — кормить СЛИМ
  (только ema_model_state_dict, /workspace/f5relay/slim_pretrain.pt);
  (г) первый скачанный model_last.pt был бит внутри при верном размере,
  двойная порча: sha сверять; трен-копии pretrained_* тоже были биты;
  (д) самостоп — ТОЛЬКО после успеха и со sleep 360.
  Чекпоинт Миты: /workspace/ckpts_mita/model_last.pt (диск сохранён).
  СЭМПЛЫ У ПОЛЬЗОВАТЕЛЯ: samples/fmita_* (24к, с ударениями и plain),
  samples/hd/fmita_* (48к AP-BWE). Инстанс остановлен, баланс $10.61.
- **ФИНАЛЬНЫЙ ВЕРДИКТ ПОЛЬЗОВАТЕЛЯ (утро 04.07): ГОЛОСОВАЯ САГА ЗАКРЫТА.**
  Чемпион — облачная Мита D («топ»). F5-файнтюн — «полнейшее говно»,
  тембр не похож (дух Миты не передался: инференс-референс всего 5.7с
  + 24кГц) — В АРХИВ, больше не копать без новых вводных. HD-апсемпл
  (AP-BWE) одобрен как инструмент — «оставим на всякий».
  Иерархия: прод = облако D + эмоции + СТТ-страж; черновик №1 = Qwen
  r28+seed123(+апсемпл); аварийный = локальный fish+референс; архив = F5.
  СЛЕДУЮЩИЙ БОЛЬШОЙ ЭТАП — ПАМЯТЬ (этап 3, см. список фич выше).

### Голос: охота за 44кГц-без-заскоков (день 04.07, продолжение)
- Пользователь: заскоки хоть и пойманы стражем — хочет класс «студия
  без костылей». Разведка дала 3 класса решений (детали в переписке):
  1) Cartesia Sonic (SSM=стабильность по конструкции, 44.1к, RU) —
     ОТВЕРГНУТ ЭКОНОМИКОЙ: клон только с Pro $5=133 мин/мес (неделя
     общения), дальше сразу $49. Ключ пользователя в .env (CARTESIA_KEY).
  2а) ПРОБА RVC (Rexsaze) ОТСЛУШАНА 04.07: тембр «Миту слышно, если
  прислушаться» (чужое сырьё модели), монотонно (наследие Silero-донора).
  Вердикт пользователя: облако D «прям идеально, будто сама Мита» — все
  локальные пути дают «похоже, но не то». Рабочий RVC-конвейер сохранён:
  /workspace/rvc310 + rvc_final.py (грабли: python3.10-dev для fairseq,
  add_safe_globals(Dictionary, Namespace) для torch>=2.6, vc_single
  возвращает (лог, (sr, audio))). Опция «своя RVC на нашем датасете» —
  только как страховка, если пользователь скомандует.
  2) RVC-конвейер (отработан, вердикт в 2а): стабильный донор → RVC-тембр
     Миты → 40-48к. ГОТОВЫЕ модели Миты найдены: Rexsaze/MitaMiSide
     (.pth+.index, RVC v2) и artooo/Miside-Mita__RVC-Voice__RUS-Version__
     (zip) на HF. Бенч: донор Silero v4 ru (48к, ударения «+») →
     rvc-python на python3.10 (uv venv; на 3.12 fairseq не встаёт;
     «pip install rvc-python» на голом venv требует setuptools).
     Скрипт /workspace/rvc_bench2.sh (переносить на новые инстансы).
  3) MiniMax Speech ПРОВЕРЕН И СКИПНУТ 04.07: ключ рабочий, образец
     залился, но клон/синтез требуют баланса, а минимальное пополнение
     $25. Реальный прайс: HD $100/М символов (≈$20-60/мес на нашем
     объёме), Turbo $60/М, клон $1.5 — как основной голос удвоил бы
     бюджет проекта. Ключ в .env (MINIMAX_KEY), file_id образца у них
     416360495718465 — если когда-нибудь появится баланс, клон в 2 запроса.
     API-грабли: purpose=voice_clone передавать form-полем (не в URL),
     аудио в ответе t2a_v2 — hex-строка.
- **ОХОТА ЗАКРЫТА ОКОНЧАТЕЛЬНО 04.07**: все три класса решений отслушаны
  или упёрлись в экономику (Cartesia — планы, RVC — тембр/монотонность,
  MiniMax — минималка $25). Корона остаётся у облачной Миты D + СТТ-страж.
  Следующий большой этап — ПАМЯТЬ (этап 3).

### Голос 3.0 кандидат: VoxCPM2 (ночь 04-05.07) — УШИ ПРОЙДЕНЫ

- Perplexity подкинул список «новых» движков — почти весь оказался нашей
  же пройденной картой (Zonos БЕЗ русского вопреки его утверждению, Silero
  без клона, XTTS/F5/CosyVoice отслушаны). Но VoxCPM2 (OpenBMB, апрель
  2026, ПОСЛЕ поста реддит-тестера про старый VoxCPM) — реально новый:
  2B, 30 языков вкл. русский, нативные 48кГц, zero-shot клон, LoRA-файнтюн.
- Бенч на 8-м инстансе (RTX 6000Ada): zero-shot с нашего 28с образца.
  Вердикты пользователя по итерациям: сырой — «звучит шикарно», но
  торопится; style-теги замедляют, НО читаются вслух (в pip-релизе voxcpm
  2.0.3 параметра стиля нет, скобки уходят в текст — проверено в исходниках);
  замедление референса/пост-стретч — «слышен слоу-мо», отвергнуто; тег
  slower + автосрез шапки — «ВСЁ СУПЕР ШИКАРНО, ИДЕАЛЬНО».
- **РЕЦЕПТ ЧЕМПИОНА** (весь код: scripts/voxcpm2_bench.py):
  тег «(Speaking very slowly, at a calm and relaxed pace)» перед текстом;
  наговоренную шапку срезать по whisper word_timestamps (первое слово
  реплики, запас 120мс, фейд-ин 20мс — без него дрожь на границе);
  torch.manual_seed(42); ударения — combining acute U+0301 («про́мах»),
  проверено stress2; СТРАХОВКА stress0 = чистый текст без разметки, если
  ударения начнут ломать слова (тоже отслушан, норм кроме словарных ошибок).
  Вариант без very (slowfix) — быстрее, на длинных «летит», не брать.
- Окружение на боксе: venv /workspace/vox (--system-site-packages, торч
  системный), pip install voxcpm librosa; faster_whisper — системный NOVA.
  На время бенча vLLM убивать (pkill-паттерны с [.], иначе pkill казнит
  сам себя), vox_bench/voxcpm2_bench добавлены в busy вачдога.
- ВПЕРЕДИ ДО КОРОНАЦИИ: (1) VRAM-бюджет — 27B мозг (0.85×48ГБ) + VoxCPM2
  (~8ГБ) на одну карту не лезут, варианты: ужать util/контекст мозга или
  карта побольше; (2) интеграция: движок VoxTTS + RUAccent для ударений
  по всему тексту + срез шапки (whisper уже есть в СТТ-страже); (3) замер
  латентности и надёжности на живом диалоге против облака D.
- GPU-рулетка: карты уводят на КАЖДОМ сне (3 раза за сутки: PRO 5000,
  A6000×2) → противоядие предложено: менее ходовые карты (A40/L40).
- Вачдог: льготный период с собственного старта (иначе казнит мгновенно
  при унаследованном idle), busy-паттерны пополнены.

### Облако fish.audio — голос ПРОДА (ночь 2026-07-03/04)

- NOVA_TTS=fishcloud, модель s2.1-pro-free ($0 до конца июля 2026),
  ключ в /workspace/fish_key, id голоса в /workspace/fish_ref_id.
- ДВЕ модели Миты на платформе: общественная 4075192824f64dc6aabbbf70124d6a01
  (один тон, но без глюков) и НАША «Mita NOVA» fa75fc40ad2c40f292f692f9e3abdbbc
  (из нашего 28с образца: богаче интонации, пользователь: «прям охуенно»,
  но редкие робо-слова). Сейчас активна НАША.
- Эмоции: мозг размечает реплики тегами [sarcastic]/[laughing]/... (S2:
  квадратные скобки, английский, в начале предложения) — см. persona;
  на экран текст идёт чистым (strip_markers), в синтез — с ремарками,
  tts_text.py прячет их от транслитерации.
- Починено: обрыв реплики при икоте облака (повтор предложения + пропуск
  только его), температура 0.7 против глюков, нормализация громкости PCM.
- Известное: паузы 3-6с перед голосом — очередь free-тарифа (без SLA),
  лечится только платным PAYG ($15/М байт).
- Варианты образца ПРОВЕРЕНЫ И ЗАКРЫТЫ (ночь 03/04.07): B (7 самых
  типичных клипов по спикер-эмбеддингам, 28.6с) — тот же заскок; C (54
  клипа, 174с) — хуже, тоны слились в нереалистичную кашу. ЧЕМПИОН — A
  (28с, 6 клипов, fa75fc40ad2c40f292f692f9e3abdbbc), стоит в проде.
  Робо-заскок ~1/20 — свойство s2.1-free на этом голосе, данными не
  лечится. Модели B/C удалены с аккаунта. Больше составов НЕ перебирать.
- Гибрид на потом: облако основным, при таймауте — автооткат на локальный
  fish (мгновенный и свой). Пара часов работы.
- Локальный fish (S1-mini + 28с референс) остаётся тёплым запасным:
  NOVA_TTS=fish, ни от чего внешнего не зависит.

### Пройденный этап: «Голос 2.0» — замена XTTS

Пользователь нашёл готовый клон голоса Миты отличного качества на платформе
fish.audio: модель «Mita Miside (Russian voice)», id в URL:
`6dc11f3f67a543f6ad4537a4a347e224` (страница:
https://fish.audio/ru/m/6dc11f3f67a543f6ad4537a4a347e224/).

Решение (согласовано): **вариант А — self-hosted**.
- Поставить на инстанс открытую модель Fish Audio **OpenAudio S1-mini**
  (репо fish-speech), вместо/рядом с XTTS, за тем же интерфейсом TTSModel
  (новый класс в nova/server/models/, конфиг через env NOVA_TTS=xtts|fish).
- Референс для клона — те же дорожки: `voices/*.ogg` (35 шт, локально,
  не в git) / `personas/nova/voice_sample.wav`.
- Сравнить с XTTS на слух, оставить лучшее.
- Запасной вариант Б: облачный API fish.audio с их готовой моделью Миты
  (максимальное качество, но платно/зависимость/приватность — пользователь
  предпочитает self-hosted).
- План Б-2 (исследование 2026-07-03), если fish не дотянет: скамейка
  сравнения зеро-шотом с нашим 28с референсом → файнтюн победителя.
  КРИТЕРИЙ ПОЛЬЗОВАТЕЛЯ: русскоязычные модели/файнтюны в приоритете,
  мультиязычные — по остаточному принципу.
  ПРОВЕРЕНО 04.07: зеро-шот F5-RU по одному клипу — «из жопы» (вердикт
  пользователя); честные причины: единичный 5.7с референс + 24кГц вокодер.
  Окружение и чекпоинт v2 остаются на инстансе (/workspace/f5env, f5ru),
  RUAccent работает отлично. Роль F5 — СТРАХОВКА: возвращаться только
  через файнтюн на полном датасете, если облако разонравится/подорожает.
  КРИТЕРИЙ ПОЛЬЗОВАТЕЛЯ (04.07, после матрицы Qwen3-TTS): облачная Мита D —
  «будто записана в студии», все локальные 24кГц (qwen все варианты) —
  «как в гарнитуре с дешёвым микрофоном; неплохо, но не то». Разрыв =
  полоса частот + класс модели. Лучшая связка qwen: реф r28 (28с концат)
  + seed 123 + temperature на вкус (t06 ровнее). CustomVoice — не для
  клона. resemble-enhance несовместим (торч 2.1/py3.11) — для апсемпла
  брать AP-BWE/FlowHigh. Решающий вопрос локалки: вернёт ли нейро-апсемпл
  «студийность». Нет — локалка закрывается, прод облако.
  РАЗВЕДКА 04.07 (реддит-тестер аудиокниг + поиск): Qwen3-TTS подтверждён
  независимо (похожесть/естественность топ, стабилен, терпим к качеству
  референса; vLLM для него проблемный — брать форк
  github.com/dffdeeq/Qwen3-TTS-streaming; веса
  Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice, 24кГц). ВЫЧЕРКНУТЫ по артефактам:
  Chatterbox, CosyVoice-3, IndexTTS2, VibeVoice, MOSS; без русского:
  Voxtral (Mistral), Zonos, EchoTTS (44кГц, но English-only), Spark, Higgs.
  Приём в конвейер (любой движок): STT-валидация — расшифровать синтез
  виспером, пересинтезировать предложения с пропусками. Сиды влияют на
  стабильность — фиксировать удачный.
  Кандидаты: 1) **Misha24-10/F5-TTS_RUSSIAN (находка пользователя, фаворит)**:
  5000+ ч RU, чекпоинт v2 со 100% разметкой ударений, ударение задаётся
  в тексте («молок+о»), автоматика — библиотека RUAccent (встроить шагом
  в tts_text.py), ~3-4 ГБ VRAM, файнтюн-конвейер F5 рабочий (датасет Миты
  готов!), CC-BY-NC; 2) ESpeech / hotstone228 (другие RU-файнтюны F5);
  3) Qwen3-TTS (RU в 10 языках, клон по 3с). Silero — не для клона.
- Альтернатива/дополнение: дообучение (fine-tune) XTTS или S1-mini на всех
  35 дорожках — субтитры сгенерирует Whisper автоматически (~1–2 ч GPU).

### Этап 3 (по спеке): память и знания

Идеи, согласованные с пользователем 2026-07-03 (обсуждение в сессии):
- **Умное дослушивание (семантический эндпоинтинг)** — сервер после ASR
  смотрит, завершена ли мысль (виспер ставит пунктуацию; «висячие»
  окончания: союзы/предлоги/«эээ»/многоточие → ждать продолжения 3–5 с,
  склеить со следующим сегментом; жёсткий потолок ~8 с). Всё на сервере,
  хрупкий аудио-клиент не трогаем. Мычание «эээ/ммм» VAD уже держит как речь.
  Поверх автоматики — голосовые маркеры пользователя: обращение с вопросом
  («...что думаешь, Нова?») = мысль закончена, отвечать на всё накопленное;
  «Нова, слушай» / «погоди, думаю» = режим накопления без таймеров (молчит
  и буферизует куски, паузы хоть минутные) до явного обращения.
- **Умный отклик (решение пользователя: БЕЗ режимов соло/компания — одно
  поведение всегда)**: имя «Нова» в сегменте = отвечает всегда; иначе LLM
  решает PASS/ответить. Принцип: гейт — «социальный инстинкт», не цензор:
  подключаться к теме, реагировать на смешное, инициировать разговор сама —
  можно и нужно; болтливость — черта характера (talkativeness), жёсткий
  кулдаун только против дребезга. Не передушить! Пользователь будет тестами
  подгонять. Цель-звезда: ведёт себя как личность, не как робот.
  В одной комнате адресат угадывается по смыслу, в Discord — точно (дорожки).
- **Обучение манере/юмору — конвейер дистилляции**: источник (VOD стримера,
  свои сессии, позже Discord) → whisper-расшифровка → LLM-выжимка (словарь
  сленга, паттерны шуток, реакции) → в персону/память сразу, пары
  «ситуация→реплика» в корпус для LoRA (этап 6). Пассивный просмотр сам по
  себе не учит — учит ночной конспект.
- **Совместный просмотр** (аниме/фильмы/игры) — главный источник эпизодической
  памяти («что смотрели, с чего орали»); для стримов/видео нужен захват
  системного звука (WASAPI loopback) — сейчас клиент слышит только микрофон.
  Дёшево сверху: уровень громкости системного звука как детектор-события
  (всплеск после тишины, смена музыки) — без ИИ, чистая амплитуда.
- **Облачные глаза (ОДОБРЕНО пользователем 04.07)**: переключатель
  NOVA_VISION=local|cloud; облаку (Gemini Flash-класс) — только кадры/клипы
  экрана, ответ текстом в контекст локального мозга. Для со-просмотра —
  козырь: видеоклипы 5–10с дают ПОНИМАНИЕ ДВИЖЕНИЯ, и облако «слышит»
  аудиодорожку клипа (диалоги фильма!) — системный звук для со-просмотра
  решается автоматически. Цена ~$0.1–0.3 за час просмотра (Flash).
  Компромисс: экран/звук фильма уходит провайдеру; слова пользователя — нет.
- **Наблюдения-паттерны → проактивные предложения** (эталон «Светки»:
  «всё катаешь соло — может, парный?»): скользящий конспект сессии
  замечает повторы (5 заездов подряд одиночные, третий час одна трасса,
  всегда сливает один поворот) → проактивный движок предлагает сам.
- **Discord (этап 4) как источник**: бот отдаёт отдельную дорожку на каждого
  участника — размеченные диалоги бесплатно; при работе в Discord локальный
  микрофон выключать (иначе пользователь слышен дважды). Друзья должны знать.

- **ВАЖНО (решение принято): память живёт на ноуте, не на инстансе.**
  `vast.py down` стягивает `memory/`, `knowledge/`, `data/feedback.jsonl`
  с инстанса (scp по уже настроенному ключу ~/.ssh/nova_vast), `vast.py up`
  заливает обратно. Иначе `--destroy`/смена карты уничтожает всё нажитое.
  (Сейчас на спящем инстансе уже лежит feedback.jsonl первого дня —
  забрать при первом же подключении.)
- Память сессий: конспект диалога при отключении → `memory/`, загрузка при
  старте (она будет помнить прошлые разговоры — пользователь этого явно ждёт).
- Сжатие переполненной истории сессии в конспект («бесконечная» память).
  Скользящее: старейший кусок истории сжимается LLM на лету и остаётся в
  контексте конспектом; свежее — дословно. Конспекты писать в базу ПО ХОДУ
  сессии, не только в конце. Приоритет источников при сжатии: слова
  пользователя > ответы NOVA > фоновый звук (аниме/фильм — агрессивнее всего,
  ~5-минутные куски в пару строк). «Запомни: ...» закрепляет дословно.
- Поднять max_model_len до 32768 (влезает; при желании 64k через
  --kv-cache-dtype fp8).
- RAG-база `knowledge/` (Chroma) + команда «запомни: ...».
- Web-самообучение: инструмент поиска, «изучи X» → конспект в базу.
- Игровые профили: minecraft, universal_game (+ гайд для новых).
- Голосовая регулировка болтливости («говори почаще/пореже»).

### Этап 4: Discord-бот (py-cord, per-user потоки, друзья слышат её).
### Этап 5: GUI настроек (PySide6, бинды, редактор персоны).
### Этап 6: LoRA-дообучение мозга по 👍/👎 (data/feedback.jsonl уже копится).

## Шпаргалка запуска (для пользователя)

    uv run python scripts/vast.py up          # поднять сервер (2-3 мин)
    uv run python scripts/vast.py status      # готовность и баланс
    uv run python -m nova.client.main         # клиент (говорить!)
    uv run python scripts/vast.py down        # закончил — погасить

Хоткеи: Ctrl+Alt+C — прокомментируй; Ctrl+Alt+M — mute; Ctrl+Alt+P — пауза;
Ctrl+Alt+↑/↓ — оценка реплики.
